# Iteration Note

## Meta

- Date: 2026-05-06
- Branch: `codex/release-0.9.4`
- PR URL: pending
- Owner: codex

## Done

- Bumped backend version to `0.9.4` in `backend/pyproject.toml`.
- Bumped frontend version to `0.9.4` in `frontend/package.json`.
- Promoted `CHANGELOG.md` unreleased entries into `## [0.9.4] - 2026-05-06`.
- Verified backend checks: `ruff`, `pytest`, `mypy` all passed.

## Blockers

- Frontend validation could not run in this environment because `npm` is unavailable in PATH.

## Next 3 Tasks

1. Open and merge release PR for `0.9.4`.
2. Run release commands from `main` (`release-prepare`, `release-publish`, `release-status`).
3. Update PR with final CI outcomes and publish artifacts confirmation.

## Risks

- If frontend dependencies or lint/build fail in CI, release publish should be paused until fixed.
