# Test Checklist

Use this checklist before merge.

- [ ] Lint passes (`ruff`)
- [ ] Tests pass (`pytest`)
- [ ] Type checks pass (`mypy`)
- [ ] API smoke check passes (`GET /health`)
- [ ] New behavior has at least one test or explicit rationale
